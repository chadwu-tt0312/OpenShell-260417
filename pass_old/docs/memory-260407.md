# Memory - 2026-04-07

## 本次目標

- 導入 per-user workspace mount 機制，讓同一份設定下可在不同時間將 `data` 指向不同 user workspace。
- 補齊可重複執行的 mount 驗證腳本。
- 排除 `bash\r`（CRLF）造成的 task 執行問題。

## 已完成的程式修改

### 1) Docker/k3s mount 入口

- 檔案：`crates/openshell-bootstrap/src/docker.rs`
- 變更：
  - 新增 `resolve_workspace_data_bind()`，讀取 `OPEN_SHELL_WORKSPACE_BASE`。
  - 解析並 canonicalize `${OPEN_SHELL_WORKSPACE_BASE}/data`。
  - 在 gateway container `HostConfig.binds` 額外加入：
    - `<resolved_base>/data:/opt/openshell/workspaces/data`

### 2) Sandbox per-user workspace mount

- 檔案：`crates/openshell-server/src/sandbox/mod.rs`
- 變更：
  - 新增 env 解析：
    - `OPEN_SHELL_WORKSPACE_BASE`
    - `OPEN_SHELL_WORKSPACE_USER`
    - `OPEN_SHELL_WORKSPACE_USER_DATAPATH`
  - 路徑解析優先序：
    1. `${BASE}/${OPEN_SHELL_WORKSPACE_USER_DATAPATH}`
    2. `${BASE}/${OPEN_SHELL_WORKSPACE_USER}_data`
    3. `${BASE}/data`
  - 將解析結果注入 podTemplate：
    - volume: `openshell-workspace-data` (`hostPath`)
    - mountPath: `/workspace/data`
  - 保留既有 supervisor sideload 行為。

### 3) 文件

- 新增：`pass/docs/per-user-workspace-mount.md`
- 說明 env 與資料流、驗證方式。

### 4) 修正 python proto task 的 bash CRLF 問題

- 檔案：`tasks/python.toml`
  - `python:proto` 改為執行 Python 腳本：
    - `run = "uv run python tasks/scripts/python_proto.py"`
- 新增：`tasks/scripts/python_proto.py`
  - 取代原 bash heredoc 做 `grpc_tools.protoc` 與 import rewrite。
- 驗證：`uv run python tasks/scripts/python_proto.py` 可成功執行。

### 5) 測試穩定性（WSL/受限環境）

- 檔案：`crates/openshell-sandbox/src/process.rs`
- 測試 `drop_privileges_succeeds_for_current_user` 已放寬：
  - 若錯誤為 `EPERM/permission denied/operation not permitted`，視為環境限制可接受。
  - 其他錯誤仍視為 fail。

### 6) mount 驗證腳本

- 新增並持續修正：`pass/openshell-mount-validate.sh`
- 現況：
  - 已加 `set -euo pipefail`
  - dump 步驟錯誤輸出會寫入 artifact
  - dump 空檔會直接 fail（不再假成功）
  - 自動清理 sandbox

## 本次關鍵觀察

1. `openshell sandbox exec` 在目前 CLI 不存在（可用子命令為 create/get/list/delete/connect/upload/download）。
2. `openshell sandbox create` 若不加命令，會進入互動 shell。
3. 先前驗證失敗原因包含：
   - `ImagePullBackOff`（image 無法拉取）
   - 指令誤用（`sandbox exec`）
   - CRLF 導致 shell 腳本異常。
4. `mise run test` 曾出現兩類問題：
   - `python:proto` 的 `bash\r`（已修）
   - 環境權限/port/特定單測問題（非全部為程式邏輯錯誤）。

## 最新驗證狀態（截至本會話）

- `pass/artifacts/mount-validation-20260407T101050Z/run.log` 顯示流程跑完。
- 但 `sandbox_a_dump.txt`、`sandbox_b_dump.txt` 為空（當時版本仍使用了不支援的 `sandbox exec`）。
- 腳本已於後續修正為遇錯即停與非空檢查，需要再跑一次取得有效結果。

## 下次會話建議第一步

1. 直接執行：
   - `bash pass/openshell-mount-validate.sh`
2. 若 image 拉取失敗先指定可用 image：
   - `export OPEN_SHELL_SANDBOX_IMAGE=ghcr.io/nvidia/openshell-community/sandboxes/base:latest`
3. 回傳以下檔案讓 AI 判讀：
   - 最新 `pass/artifacts/mount-validation-*/run.log`
   - 最新 `sandbox_a_dump.txt`
   - 最新 `sandbox_b_dump.txt`

## 驗證通過標準

- `run.log` 無中途中止錯誤。
- `sandbox_a_dump.txt` 有 `/workspace/data` 的檔案內容，且對應 `data_user01`。
- `sandbox_b_dump.txt` 有 `/workspace/data` 的檔案內容，且對應 `data`。
- 兩者內容可明確區分來源（例如 `this is data_user01 dir` vs `this is data dir`）。

## 重要環境變數（本次使用）

- `OPEN_SHELL_WORKSPACE_BASE=./pass`
- `OPEN_SHELL_WORKSPACE_USER=user01`
- `OPEN_SHELL_WORKSPACE_USER_DATAPATH=data_user01`

